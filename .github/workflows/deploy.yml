# AUTO-GENERATED — do not edit directly.
# Source:  theholocron/holocron · packages/cli/src/commands/setup-workflows.ts
# Tool:    holocron setup
# Changes: run `holocron setup` to regenerate.
name: Deploy

on: # yamllint disable-line rule:truthy
  push:
    branches: [main]
    paths:
      - docs/**
      - apps/web/src/**
      - apps/web/.storybook/**
      - packages/ui/src/**
      - packages/ui/.storybook/**
      - astro.config.ts
      - pnpm-workspace.yaml
      - pnpm-lock.yaml
  workflow_dispatch:

concurrency:
  group: pages
  cancel-in-progress: false

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    name: Deploy
    uses: theholocron/.github/.github/workflows/deploy.yml@main
    with:
      type: docs
      name: monorepo-nextjs-template
      storybook-projects: '[{"name":"web","workingDir":"apps/web"},{"name":"ui","workingDir":"packages/ui"}]'
    secrets: inherit
